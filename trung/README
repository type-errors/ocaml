- To compile:

    git clone https://github.com/type-errors/ocaml/
    cd ocaml
    git fetch
    git checkout 4.06
    ./configure
    make world.opt

- To run OCaml compiler after compilation:

    ./byterun/ocamlrun ./ocamlc -I stdlib ../ocaml/examples/foo.ml

- See more at the RUNTOP environment in the Makefile